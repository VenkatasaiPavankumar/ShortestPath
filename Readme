1. Make sure java is installed on the machine.
2. Open the terminal in the project root folder i.e ShortestPath
3. Run the command "javac src/ShortestPathAlgorithmFinder.java"
4. Run the command "java -cp src ShortestPathAlgorithmFinder"